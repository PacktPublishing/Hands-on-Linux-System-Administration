<Directory "/var/www/html/secret">
	AuthType Basic
	AuthName "Super secret directory"
	# Change the path below to /etc/apache2/.htpasswd on Debian & Ubuntu
	AuthUserFile /etc/httpd/.htpasswd
	Require valid-user
</Directory>
